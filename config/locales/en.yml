en:
  activerecord:
    errors:
      models:
        user:
          invalid_format: "must be 3-20 characters long, use only lowercase letters, numbers, and non-consecutive special characters (., _, -). Must start and end with a letter or number."
