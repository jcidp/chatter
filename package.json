{
  "name": "chatter",
  "version": "1.0.0",
  "engines": {
    "node": "20.17.0"
  },
  "scripts": {
    "clear": "rm -rf public/",
    "build": "npm install --prefix client/ && npm run build --prefix client/",
    "deploy": "npm run clear && cp -a client/dist/. public/",
    "build-deploy": "npm run build && npm run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
